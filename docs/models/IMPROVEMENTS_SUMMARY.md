# 모델 개선 요약

## 📊 평가 지표 분석

평가 코드를 분석한 결과:
- **F1 Score (60% 가중치)**: 정답 쌍과 제출 쌍의 일치도
  - Precision = tp / (tp + fp) → FP를 줄여야 함
  - Recall = tp / (tp + fn) → FN을 줄여야 함
- **NMAE (40% 가중치)**: 예측값의 정확도
  - 정답에 있고 제출에도 있으면: |y_true - y_pred| / (|y_true| + eps)
  - 정답에만 있거나 제출에만 있으면: 오차 100%

## 🔧 개선 사항

### 1. preprocessing 모델 개선

#### 쌍 선택 로직 개선
- ✅ **안정성 점수 추가**: 시계열의 변동계수(CV) 기반 안정성 계산
- ✅ **이중 필터링**: 최소 상관계수(0.3) + 임계값(0.4) 조건
- ✅ **종합 점수**: 상관계수(50%) + 안정성(30%) + 공급망 점수(20%)
- ✅ **상위 N개 선택**: 종합 점수로 정렬 후 상위 쌍만 선택 가능

#### 예측 모델 개선
- ✅ **Ridge 정규화**: LinearRegression → Ridge (alpha=1.0)
- ✅ **Feature Scaling**: StandardScaler로 feature 정규화
- ✅ **과적합 방지**: 정규화로 일반화 성능 향상

### 2. ev_battery 모델 개선

#### 쌍 선택 로직 개선
- ✅ **이중 필터링**: 최소 상관계수(0.25) + 가중치 적용 임계값(0.4)
- ✅ **노이즈 방지**: 너무 낮은 상관계수 제외

#### 예측 모델 개선
- ✅ **Ridge 정규화**: LinearRegression → Ridge (alpha=1.0)
- ✅ **Feature Scaling**: StandardScaler로 feature 정규화
- ✅ **과적합 방지**: 정규화로 일반화 성능 향상

## 📈 예상 효과

### F1 Score 개선
1. **안정성 점수**: 변동성이 낮은 안정적인 쌍 우선 선택 → FP 감소
2. **이중 필터링**: 최소 상관계수 조건으로 노이즈 제거 → FP 감소
3. **종합 점수**: 더 정확한 쌍 선택 → Precision 향상

### NMAE 개선
1. **Feature Scaling**: feature 간 스케일 차이 해결 → 예측 정확도 향상
2. **Ridge 정규화**: 과적합 방지로 일반화 성능 향상 → 예측 정확도 향상
3. **안정적인 쌍 선택**: 예측하기 쉬운 쌍 우선 → 예측 정확도 향상

## 🎯 주요 변경사항

### preprocessing 모델
```python
# Before
if abs(best_corr) >= 0.4:
    채택

# After
if abs(best_corr) >= 0.3 and abs(best_corr) >= 0.4:
    composite_score = abs(best_corr) * 0.5 + stability * 0.3 + supply_chain * 0.2
    채택 (종합 점수로 정렬 가능)
```

### ev_battery 모델
```python
# Before
if effective_corr >= 0.4:
    채택

# After
if abs(best_corr) >= 0.25 and effective_corr >= 0.4:
    채택 (최소 상관계수 조건 추가)
```

### 예측 모델 (공통)
```python
# Before
reg = LinearRegression()
reg.fit(train_X, train_y)

# After
scaler = StandardScaler()
train_X_scaled = scaler.fit_transform(train_X)
reg = Ridge(alpha=1.0)
reg.fit(train_X_scaled, train_y)
```

## 📝 하이퍼파라미터 조정 가능

1. **min_abs_corr**: 최소 상관계수 조건 (현재: preprocessing=0.3, ev_battery=0.25)
2. **Ridge alpha**: 정규화 강도 (현재: 1.0)
3. **max_pairs**: 최대 쌍 개수 (선택사항, None이면 모든 쌍 사용)
4. **종합 점수 가중치**: 상관계수/안정성/공급망 점수 비율

## 🔍 다음 단계

1. **성능 평가**: 개선된 모델로 제출 파일 생성 후 평가
2. **하이퍼파라미터 튜닝**: alpha, min_abs_corr 등 조정
3. **앙상블**: 두 모델의 예측 결합 고려

