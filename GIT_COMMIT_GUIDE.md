# Git 커밋 및 잔디 심기 가이드

GitHub 잔디를 심기 위한 일일 커밋 가이드입니다.

## 🌱 잔디 심기란?

GitHub 프로필에 표시되는 **커밋 활동 그래프**를 말합니다. 매일 커밋을 하면 초록색으로 표시되어 "잔디를 심는다"고 표현합니다.

---

## 📋 기본 커밋 절차

### 1단계: 변경사항 확인

```bash
git status
```

**결과 예시:**
```
On branch main
Changes not staged for commit:
  modified:   analysis/preprocessing.ipynb
  modified:   docs/analysis/PREPROCESSING_GUIDE.md

Untracked files:
  docs/analysis/PREPROCESS_EV_BATTERY_GUIDE.md
```

**의미:**
- `modified`: 수정된 파일
- `Untracked files`: 새로 추가된 파일

---

### 2단계: 변경사항 스테이징 (추가)

```bash
git add .
```

**의미:**
- `.` (점)은 현재 디렉토리의 모든 변경사항을 의미
- 특정 파일만 추가하려면: `git add 파일명`

**예시:**
```bash
# 모든 변경사항 추가
git add .

# 특정 파일만 추가
git add docs/analysis/PREPROCESSING_GUIDE.md

# 특정 폴더만 추가
git add docs/
```

---

### 3단계: 커밋 메시지 작성

```bash
git commit -m '커밋 메시지'
```

**좋은 커밋 메시지 예시:**
```bash
# 간단한 설명
git commit -m '전처리 가이드 문서 추가'

# 더 자세한 설명
git commit -m 'docs: preprocessing.ipynb 전처리 과정 가이드 문서 작성'

# 여러 줄 메시지
git commit -m '전처리 가이드 문서 추가

- preprocessing.ipynb 전처리 과정 상세 설명
- 8단계 전처리 프로세스 문서화
- EDA 및 분석 섹션 추가'
```

**커밋 메시지 컨벤션:**
- `feat:` 새로운 기능 추가
- `fix:` 버그 수정
- `docs:` 문서 수정
- `style:` 코드 포맷팅
- `refactor:` 코드 리팩토링
- `test:` 테스트 추가
- `chore:` 기타 작업

---

### 4단계: 원격 저장소에 푸시

```bash
git push
```

**의미:**
- 로컬 커밋을 GitHub 원격 저장소에 업로드
- 첫 푸시 시 브랜치 설정이 필요할 수 있음

**첫 푸시 시:**
```bash
git push -u origin main
# 또는
git push -u origin master
```

---

## 🚀 완전한 예시

### 시나리오: 문서 파일을 수정하고 커밋하기

```bash
# 1. 현재 상태 확인
git status

# 2. 변경사항 추가
git add .

# 3. 커밋 (메시지 작성)
git commit -m 'docs: 전처리 가이드 문서 업데이트'

# 4. 원격 저장소에 푸시
git push
```

---

## 💡 일일 커밋 팁

### 1. 작은 변경이라도 커밋하기

```bash
# 문서 오타 수정
git add .
git commit -m 'docs: 오타 수정'
git push

# 주석 추가
git add .
git commit -m 'chore: 코드 주석 추가'
git push

# 파일 정리
git add .
git commit -m 'chore: 불필요한 파일 삭제'
git push
```

### 2. 작업 단위별로 커밋하기

```bash
# 기능 1 완료
git add feature1.py
git commit -m 'feat: 기능 1 구현'
git push

# 기능 2 완료
git add feature2.py
git commit -m 'feat: 기능 2 구현'
git push
```

### 3. 하루 종료 시 커밋하기

```bash
# 하루 작업 마무리
git add .
git commit -m 'chore: 오늘 작업 마무리'
git push
```

---

## 🔧 Git 저장소 초기화 (처음 한 번만)

### 저장소가 없는 경우

```bash
# 1. Git 저장소 초기화
git init

# 2. 원격 저장소 연결 (GitHub에서 생성한 저장소 URL)
git remote add origin https://github.com/사용자명/저장소명.git

# 3. 첫 커밋
git add .
git commit -m 'Initial commit'

# 4. 첫 푸시
git push -u origin main
```

---

## 📝 자주 사용하는 Git 명령어

### 상태 확인
```bash
git status          # 현재 상태 확인
git log             # 커밋 히스토리 확인
git log --oneline   # 간단한 커밋 히스토리
```

### 브랜치 관리
```bash
git branch                    # 브랜치 목록 확인
git branch 브랜치명          # 새 브랜치 생성
git checkout 브랜치명         # 브랜치 전환
git checkout -b 브랜치명      # 브랜치 생성 및 전환
```

### 변경사항 관리
```bash
git add .                     # 모든 변경사항 추가
git add 파일명                # 특정 파일만 추가
git reset 파일명              # 스테이징 취소
git diff                      # 변경사항 확인
```

### 커밋 관리
```bash
git commit -m '메시지'        # 커밋 생성
git commit --amend            # 마지막 커밋 수정
git reset --soft HEAD~1       # 마지막 커밋 취소 (변경사항 유지)
```

### 원격 저장소
```bash
git push                      # 원격 저장소에 푸시
git pull                      # 원격 저장소에서 가져오기
git fetch                     # 원격 저장소 정보 가져오기
```

---

## ⚠️ 주의사항

### 1. 커밋 전 확인하기
```bash
# 커밋 전에 항상 상태 확인
git status
git diff                      # 변경사항 확인
```

### 2. 중요한 파일은 .gitignore에 추가
```bash
# .gitignore 파일에 추가
echo "*.pyc" >> .gitignore
echo "__pycache__/" >> .gitignore
echo ".env" >> .gitignore
```

### 3. 커밋 메시지는 명확하게
```bash
# ❌ 나쁜 예
git commit -m '수정'

# ✅ 좋은 예
git commit -m 'docs: 전처리 가이드 문서 추가'
```

---

## 🎯 일일 커밋 체크리스트

- [ ] `git status`로 변경사항 확인
- [ ] `git add .`로 변경사항 추가
- [ ] `git commit -m '의미있는 메시지'`로 커밋
- [ ] `git push`로 원격 저장소에 푸시
- [ ] GitHub에서 커밋 확인

---

## 📚 추가 학습 자료

- [Git 공식 문서](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Pro Git 책](https://git-scm.com/book/ko/v2)

---

**작성일**: 2024-11-18  
**목적**: 일일 커밋으로 GitHub 잔디 심기

